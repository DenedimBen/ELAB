name: flutter_application_1
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.10.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  intl: ^0.20.2  # flutter_localizations için gerekli (pinned by SDK)
  shared_preferences: ^2.2.2  # Seçilen dili hafızada tutmak için ve Pro üyeliği hafızada tutmak için
  google_mobile_ads: ^5.1.0  # Reklamlar için
  # Veritabanı ve Dosya İşlemleri
  sqflite: ^2.3.0           # Yerel veritabanı (SQL)
  path: ^1.9.0              # Dosya yolları için
  spreadsheet_decoder: ^2.2.0 # Excel dosyasını okumak için (En kolayı bu)

  # Görsellik ve Animasyon
  lottie: ^3.1.0            # Kalp atışı vb. efektler için
  google_fonts: ^6.1.0      # Modern yazı tipleri için
  flutter_svg: ^2.0.9       # Vektörel ikonlar için
  audioplayers: ^6.0.0      # Ses çalmak için
  font_awesome_flutter: ^10.6.0 # Ek ikon seti

  # State Management (Durum Yönetimi)
  provider: ^6.1.1          # Sayfalar arası veri taşıma

  # Navigation (Alt Menü)
  google_nav_bar: ^5.0.6    # Modern alt navigasyon

  # Firebase ve Cloud Paketi
  firebase_core: ^2.27.0      # Firebase Beyni
  firebase_auth: ^4.17.8      # Giriş/Çıkış İşlemleri
  google_sign_in: ^6.2.1      # Google Butonu Mantığı
  cloud_firestore: ^4.15.8    # Veritabanı (Favoriler ve Topluluk için)
  firebase_storage: ^11.6.9   # Dosya yükleme
  image_picker: ^1.0.7        # Galeriden resim seçme
  
  # Forum Performans ve UI Paketleri
  cached_network_image: ^3.3.0  # Resimleri önbelleğe alır ve hızlı yükler
  timeago: ^3.6.1               # "3 dakika önce" formatı
  
  # Localization ve I18n
  flutter_gen: ^5.3.2           # Localization kodları oluşturmak için

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_gen_runner: ^5.3.2  # Dil dosyalarını otomatik generate etmek için
  
  # İKON VE SPLASH İÇİN GEREKLİ PAKETLER:
  flutter_launcher_icons: ^0.13.1
  flutter_native_splash: ^2.3.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true # Otomatik dosya üretimi için (l10n)
  uses-material-design: true
  assets:
    - assets/images/
    - assets/packages/
    - assets/animations/
    - assets/db/
    - assets/sounds/
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# --- UYGULAMA İKONU AYARLARI ---
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/app_icon.png"
  min_sdk_android: 21 # Android sürümü

# --- AÇILIŞ EKRANI AYARLARI ---
flutter_native_splash:
  color: "#2E3239" # Bizim koyu tema rengimiz
  image: "assets/images/app_icon.png"
  android_12:
    image: "assets/images/app_icon.png"
    color: "#2E3239"
